'use strict';
// const  = 2000;
function clacAge(birthyear) {
  const age = 2024 - birthyear;
//    let total=100;
  return age;
}
clacAge(2000);
function laterage(total) {
  if (clacAge == 2000) {
    // total = this.total - 300;
    console.log('📉 Profit!', total);
  } else {
    console.log('📈 Loss!', total - 30);
  }
}
const laterage2 = laterage(100);
// console.log(laterage2)
